#unzip the file

#navigate to the file location on your path(do this on your terminal window) for example /users/desktop/afrilearn-task

#run npm install --make sure you have an active internet connection

#run npm start

#navigate to the client folder on your terminal(open another terminal window) for example /users/desktop/afrilearn-task/client

#run npm install

#run npm start

#hurray -- the project is now avanilable for you to view on your localhost. http://localhost:3000